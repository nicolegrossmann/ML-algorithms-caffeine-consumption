# Raw Data Files

Place the RNA-seq files here before running notebooks.

Expected files:
- `expression_matrix.csv`
- `metadata.csv`

Use `expression_matrix_template.csv` and `metadata_template.csv` as schema examples.
